/*
 * Whisparr
 *
 * Whisparr API docs
 *
 * The version of the OpenAPI document: 3.0.0
 * Generated by: https://github.com/openapitools/openapi-generator.git
 */

#nullable enable

using System;
using System.IO;
using System.Linq;
using System.Collections;
using System.Collections.Generic;
using System.Text;
using System.Text.Json;
using System.Text.RegularExpressions;
using Whisparr3.Net.Model;
using System.Runtime.CompilerServices;
using System.Net.Http.Headers;

[assembly: InternalsVisibleTo("Whisparr3.Net.Test")]

namespace Whisparr3.Net.Client
{
    /// <summary>
    /// Utility functions providing some benefit to API client consumers.
    /// </summary>
    public static partial class ClientUtils
    {

        /// <summary>
        /// A delegate for events.
        /// </summary>
        /// <typeparam name="T"></typeparam>
        /// <param name="sender"></param>
        /// <param name="e"></param>
        /// <returns></returns>
        public delegate void EventHandler<T>(object sender, T e) where T : EventArgs;

        /// <summary>
        /// An enum of headers
        /// </summary>
        public enum ApiKeyHeader
        {
            /// <summary>
            /// The X-Api-Key header
            /// </summary>
            X_Api_Key,
            /// <summary>
            /// The apikey header
            /// </summary>
            Apikey
        }

        /// <summary>
        /// Converte an ApiKeyHeader to a string
        /// </summary>
        /// <param name="value"></param>
        /// <returns></returns>
        /// <exception cref="System.ComponentModel.InvalidEnumArgumentException"></exception>
        public static string ApiKeyHeaderToString(ApiKeyHeader value)
        {
            return value switch
            {
                ApiKeyHeader.X_Api_Key => "X-Api-Key",
                ApiKeyHeader.Apikey => "apikey",
                _ => throw new System.ComponentModel.InvalidEnumArgumentException(nameof(value), (int)value, typeof(ApiKeyHeader)),
            };
        }

        /// <summary>
        /// Returns true when deserialization succeeds.
        /// </summary>
        /// <typeparam name="T"></typeparam>
        /// <param name="json"></param>
        /// <param name="options"></param>
        /// <param name="result"></param>
        /// <returns></returns>
        public static bool TryDeserialize<T>(string json, JsonSerializerOptions options, [global::System.Diagnostics.CodeAnalysis.NotNullWhen(true)] out T? result)
        {
            try
            {
                result = JsonSerializer.Deserialize<T>(json, options);
                return result != null;
            }
            catch (Exception)
            {
                result = default;
                return false;
            }
        }

        /// <summary>
        /// Returns true when deserialization succeeds.
        /// </summary>
        /// <typeparam name="T"></typeparam>
        /// <param name="reader"></param>
        /// <param name="options"></param>
        /// <param name="result"></param>
        /// <returns></returns>
        public static bool TryDeserialize<T>(ref Utf8JsonReader reader, JsonSerializerOptions options, [global::System.Diagnostics.CodeAnalysis.NotNullWhen(true)] out T? result)
        {
            try
            {
                result = JsonSerializer.Deserialize<T>(ref reader, options);
                return result != null;
            }
            catch (Exception)
            {
                result = default;
                return false;
            }
        }

        /// <summary>
        /// If parameter is DateTime, output in a formatted string (default ISO 8601), customizable with Configuration.DateTime.
        /// If parameter is a list, join the list with ",".
        /// Otherwise just return the string.
        /// </summary>
        /// <param name="obj">The parameter (header, path, query, form).</param>
        /// <param name="format">The DateTime serialization format.</param>
        /// <returns>Formatted string.</returns>
        public static string? ParameterToString(object? obj, string? format = ISO8601_DATETIME_FORMAT)
        {
            if (obj is DateTime dateTime)
                // Return a formatted date string - Can be customized with Configuration.DateTimeFormat
                // Defaults to an ISO 8601, using the known as a Round-trip date/time pattern ("o")
                // https://msdn.microsoft.com/en-us/library/az4se3k1(v=vs.110).aspx#Anchor_8
                // For example: 2009-06-15T13:45:30.0000000
                return dateTime.ToString(format);
            if (obj is DateTimeOffset dateTimeOffset)
                // Return a formatted date string - Can be customized with Configuration.DateTimeFormat
                // Defaults to an ISO 8601, using the known as a Round-trip date/time pattern ("o")
                // https://msdn.microsoft.com/en-us/library/az4se3k1(v=vs.110).aspx#Anchor_8
                // For example: 2009-06-15T13:45:30.0000000
                return dateTimeOffset.ToString(format);
            if (obj is DateOnly dateOnly)
                return dateOnly.ToString(format);
            if (obj is bool boolean)
                return boolean
                    ? "true"
                    : "false";
            if (obj is AddMovieMethod addMovieMethod)
                return AddMovieMethodValueConverter.ToJsonValue(addMovieMethod);
            if (obj is ApplyTags applyTags)
                return ApplyTagsValueConverter.ToJsonValue(applyTags);
            if (obj is AuthenticationRequiredType authenticationRequiredType)
                return AuthenticationRequiredTypeValueConverter.ToJsonValue(authenticationRequiredType);
            if (obj is AuthenticationType authenticationType)
                return AuthenticationTypeValueConverter.ToJsonValue(authenticationType);
            if (obj is BackupType backupType)
                return BackupTypeValueConverter.ToJsonValue(backupType);
            if (obj is BreastTypeEnum breastTypeEnum)
                return BreastTypeEnumValueConverter.ToJsonValue(breastTypeEnum);
            if (obj is CertificateValidationType certificateValidationType)
                return CertificateValidationTypeValueConverter.ToJsonValue(certificateValidationType);
            if (obj is ColonReplacementFormat colonReplacementFormat)
                return ColonReplacementFormatValueConverter.ToJsonValue(colonReplacementFormat);
            if (obj is CommandPriority commandPriority)
                return CommandPriorityValueConverter.ToJsonValue(commandPriority);
            if (obj is CommandResult commandResult)
                return CommandResultValueConverter.ToJsonValue(commandResult);
            if (obj is CommandStatus commandStatus)
                return CommandStatusValueConverter.ToJsonValue(commandStatus);
            if (obj is CommandTrigger commandTrigger)
                return CommandTriggerValueConverter.ToJsonValue(commandTrigger);
            if (obj is CreditType creditType)
                return CreditTypeValueConverter.ToJsonValue(creditType);
            if (obj is DatabaseType databaseType)
                return DatabaseTypeValueConverter.ToJsonValue(databaseType);
            if (obj is DownloadProtocol downloadProtocol)
                return DownloadProtocolValueConverter.ToJsonValue(downloadProtocol);
            if (obj is Ethnicity ethnicity)
                return EthnicityValueConverter.ToJsonValue(ethnicity);
            if (obj is ExtraFileType extraFileType)
                return ExtraFileTypeValueConverter.ToJsonValue(extraFileType);
            if (obj is EyeColor eyeColor)
                return EyeColorValueConverter.ToJsonValue(eyeColor);
            if (obj is FileDateType fileDateType)
                return FileDateTypeValueConverter.ToJsonValue(fileDateType);
            if (obj is Gender gender)
                return GenderValueConverter.ToJsonValue(gender);
            if (obj is HairColor hairColor)
                return HairColorValueConverter.ToJsonValue(hairColor);
            if (obj is HealthCheckResult healthCheckResult)
                return HealthCheckResultValueConverter.ToJsonValue(healthCheckResult);
            if (obj is ImportExclusionReason importExclusionReason)
                return ImportExclusionReasonValueConverter.ToJsonValue(importExclusionReason);
            if (obj is ImportExclusionType importExclusionType)
                return ImportExclusionTypeValueConverter.ToJsonValue(importExclusionType);
            if (obj is ImportListType importListType)
                return ImportListTypeValueConverter.ToJsonValue(importListType);
            if (obj is ItemType itemType)
                return ItemTypeValueConverter.ToJsonValue(itemType);
            if (obj is MediaCoverTypes mediaCoverTypes)
                return MediaCoverTypesValueConverter.ToJsonValue(mediaCoverTypes);
            if (obj is MonitorTypes monitorTypes)
                return MonitorTypesValueConverter.ToJsonValue(monitorTypes);
            if (obj is MovieHistoryEventType movieHistoryEventType)
                return MovieHistoryEventTypeValueConverter.ToJsonValue(movieHistoryEventType);
            if (obj is MovieMetadataType movieMetadataType)
                return MovieMetadataTypeValueConverter.ToJsonValue(movieMetadataType);
            if (obj is MovieRuntimeFormatType movieRuntimeFormatType)
                return MovieRuntimeFormatTypeValueConverter.ToJsonValue(movieRuntimeFormatType);
            if (obj is MovieStatusType movieStatusType)
                return MovieStatusTypeValueConverter.ToJsonValue(movieStatusType);
            if (obj is PerformerStatus performerStatus)
                return PerformerStatusValueConverter.ToJsonValue(performerStatus);
            if (obj is PrivacyLevel privacyLevel)
                return PrivacyLevelValueConverter.ToJsonValue(privacyLevel);
            if (obj is ProperDownloadTypes properDownloadTypes)
                return ProperDownloadTypesValueConverter.ToJsonValue(properDownloadTypes);
            if (obj is ProviderMessageType providerMessageType)
                return ProviderMessageTypeValueConverter.ToJsonValue(providerMessageType);
            if (obj is ProxyType proxyType)
                return ProxyTypeValueConverter.ToJsonValue(proxyType);
            if (obj is QualitySource qualitySource)
                return QualitySourceValueConverter.ToJsonValue(qualitySource);
            if (obj is QueueStatus queueStatus)
                return QueueStatusValueConverter.ToJsonValue(queueStatus);
            if (obj is RatingType ratingType)
                return RatingTypeValueConverter.ToJsonValue(ratingType);
            if (obj is RejectionType rejectionType)
                return RejectionTypeValueConverter.ToJsonValue(rejectionType);
            if (obj is RescanAfterRefreshType rescanAfterRefreshType)
                return RescanAfterRefreshTypeValueConverter.ToJsonValue(rescanAfterRefreshType);
            if (obj is RuntimeMode runtimeMode)
                return RuntimeModeValueConverter.ToJsonValue(runtimeMode);
            if (obj is SearchDateFormatType searchDateFormatType)
                return SearchDateFormatTypeValueConverter.ToJsonValue(searchDateFormatType);
            if (obj is SearchStudioFormatType searchStudioFormatType)
                return SearchStudioFormatTypeValueConverter.ToJsonValue(searchStudioFormatType);
            if (obj is SortDirection sortDirection)
                return SortDirectionValueConverter.ToJsonValue(sortDirection);
            if (obj is SourceType sourceType)
                return SourceTypeValueConverter.ToJsonValue(sourceType);
            if (obj is StudioStatus studioStatus)
                return StudioStatusValueConverter.ToJsonValue(studioStatus);
            if (obj is TrackedDownloadState trackedDownloadState)
                return TrackedDownloadStateValueConverter.ToJsonValue(trackedDownloadState);
            if (obj is TrackedDownloadStatus trackedDownloadStatus)
                return TrackedDownloadStatusValueConverter.ToJsonValue(trackedDownloadStatus);
            if (obj is UpdateMechanism updateMechanism)
                return UpdateMechanismValueConverter.ToJsonValue(updateMechanism);
            if (obj is ICollection collection)
            {
                List<string?> entries = new();
                foreach (var entry in collection)
                    entries.Add(ParameterToString(entry));
                return string.Join(",", entries);
            }

            return Convert.ToString(obj, System.Globalization.CultureInfo.InvariantCulture);
        }

        /// <summary>
        /// URL encode a string
        /// Credit/Ref: https://github.com/restsharp/RestSharp/blob/master/RestSharp/Extensions/StringExtensions.cs#L50
        /// </summary>
        /// <param name="input">string to be URL encoded</param>
        /// <returns>Byte array</returns>
        public static string UrlEncode(string input)
        {
            const int maxLength = 32766;

            if (input == null)
            {
                throw new ArgumentNullException("input");
            }

            if (input.Length <= maxLength)
            {
                return Uri.EscapeDataString(input);
            }

            StringBuilder sb = new StringBuilder(input.Length * 2);
            int index = 0;

            while (index < input.Length)
            {
                int length = Math.Min(input.Length - index, maxLength);
                string subString = input.Substring(index, length);

                sb.Append(Uri.EscapeDataString(subString));
                index += subString.Length;
            }

            return sb.ToString();
        }

        /// <summary>
        /// Encode string in base64 format.
        /// </summary>
        /// <param name="text">string to be encoded.</param>
        /// <returns>Encoded string.</returns>
        public static string Base64Encode(string text)
        {
            return Convert.ToBase64String(global::System.Text.Encoding.UTF8.GetBytes(text));
        }

        /// <summary>
        /// Convert stream to byte array
        /// </summary>
        /// <param name="inputStream">Input stream to be converted</param>
        /// <returns>Byte array</returns>
        public static byte[] ReadAsBytes(Stream inputStream)
        {
            using (var ms = new MemoryStream())
            {
                inputStream.CopyTo(ms);
                return ms.ToArray();
            }
        }

        /// <summary>
        /// Select the Content-Type header's value from the given content-type array:
        /// if JSON type exists in the given array, use it;
        /// otherwise use the first one defined in 'consumes'
        /// </summary>
        /// <param name="contentTypes">The Content-Type array to select from.</param>
        /// <returns>The Content-Type header to use.</returns>
        public static string? SelectHeaderContentType(string[] contentTypes)
        {
            if (contentTypes.Length == 0)
                return null;

            foreach (var contentType in contentTypes)
            {
                if (IsJsonMime(contentType))
                    return contentType;
            }

            return contentTypes[0]; // use the first content type specified in 'consumes'
        }

        /// <summary>
        /// Select the Accept header's value from the given accepts array:
        /// if JSON exists in the given array, use it;
        /// otherwise use all of them (joining into a string)
        /// </summary>
        /// <param name="accepts">The accepts array to select from.</param>
        /// <returns>The Accept header to use.</returns>
        public static string? SelectHeaderAccept(string[] accepts)
        {
            if (accepts.Length == 0)
                return null;

            if (accepts.Contains("application/json", StringComparer.OrdinalIgnoreCase))
                return "application/json";

            return string.Join(",", accepts);
        }

        

        /// <summary>
        /// Select the Accept header's value from the given accepts array:
        /// if JSON exists in the given array, use it;
        /// otherwise use all of them.
        /// </summary>
        /// <param name="accepts">The accepts array to select from.</param>
        /// <returns>The Accept header values to use.</returns>
        public static IEnumerable<MediaTypeWithQualityHeaderValue> SelectHeaderAcceptArray(string[] accepts)
        {
            if (accepts.Length == 0)
                    return [];

            if (accepts.Contains("application/json", StringComparer.OrdinalIgnoreCase))
                    return [MediaTypeWithQualityHeaderValue.Parse("application/json")];

            return accepts.Select(MediaTypeWithQualityHeaderValue.Parse);
        }

        /// <summary>
        /// Provides a case-insensitive check that a provided content type is a known JSON-like content type.
        /// </summary>
        [GeneratedRegex("(?i)^(application/json|[^;/ \t]+/[^;/ \t]+[+]json)[ \t]*(;.*)?$")]
        private static partial Regex JsonRegex();

        /// <summary>
        /// Check if the given MIME is a JSON MIME.
        /// JSON MIME examples:
        ///    application/json
        ///    application/json; charset=UTF8
        ///    APPLICATION/JSON
        ///    application/vnd.company+json
        /// </summary>
        /// <param name="mime">MIME</param>
        /// <returns>Returns True if MIME type is json.</returns>
        public static bool IsJsonMime(string mime)
        {
            if (string.IsNullOrWhiteSpace(mime)) return false;

            return JsonRegex().IsMatch(mime) || mime.Equals("application/json-patch+json");
        }

        /// <summary>
        /// Get the discriminator
        /// </summary>
        /// <param name="utf8JsonReader"></param>
        /// <param name="discriminator"></param>
        /// <returns></returns>
        /// <exception cref="JsonException"></exception>
        public static string? GetDiscriminator(Utf8JsonReader utf8JsonReader, string discriminator)
        {
            int currentDepth = utf8JsonReader.CurrentDepth;

            if (utf8JsonReader.TokenType != JsonTokenType.StartObject && utf8JsonReader.TokenType != JsonTokenType.StartArray)
                throw new JsonException();

            JsonTokenType startingTokenType = utf8JsonReader.TokenType;

            while (utf8JsonReader.Read())
            {
                if (startingTokenType == JsonTokenType.StartObject && utf8JsonReader.TokenType == JsonTokenType.EndObject && currentDepth == utf8JsonReader.CurrentDepth)
                    break;

                if (startingTokenType == JsonTokenType.StartArray && utf8JsonReader.TokenType == JsonTokenType.EndArray && currentDepth == utf8JsonReader.CurrentDepth)
                    break;

                if (utf8JsonReader.TokenType == JsonTokenType.PropertyName && currentDepth == utf8JsonReader.CurrentDepth - 1)
                {
                    string? localVarJsonPropertyName = utf8JsonReader.GetString();
                    utf8JsonReader.Read();

                    if (localVarJsonPropertyName != null && localVarJsonPropertyName.Equals(discriminator))
                        return utf8JsonReader.GetString();
                }
            }

            throw new JsonException("The specified discriminator was not found.");
        }

        /// <summary>
        /// Determines if the provided header is a content header
        /// </summary>
        /// <param name="header">The header to check</param>
        /// <returns>True if a content header; False otherwise</returns>
        public static bool IsContentHeader(string header)
        {
            return ContentHeaders.Contains(header.ToLowerInvariant());
        }

        /// <summary>
        /// The collection of content headers as per
        /// https://learn.microsoft.com/en-us/dotnet/api/system.net.http.httpcontent.headers
        /// </summary>
        private static readonly string[] ContentHeaders = new String[] 
        {
            "allow",
            "content-encoding",
            "content-disposition",
            "content-language",
            "content-length",
            "content-location",
            "content-md5",
            "content-range",
            "content-type",
            "expires",
            "last-modified"
        };

        /// <summary>
        /// The base path of the API
        /// </summary>
        public const string BASE_ADDRESS = "http://localhost:6969";

        /// <summary>
        /// The scheme of the API
        /// </summary>
        public const string SCHEME = "http";

        /// <summary>
        /// The context path of the API
        /// </summary>
        public const string CONTEXT_PATH = "";

        /// <summary>
        /// The host of the API
        /// </summary>
        public const string HOST = "localhost";

        /// <summary>
        /// The format to use for DateTime serialization
        /// </summary>
        public const string ISO8601_DATETIME_FORMAT = "o";
    }
}
